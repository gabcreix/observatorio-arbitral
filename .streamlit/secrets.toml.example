# Para desplegar en Streamlit Community Cloud (D34): copia esto en
# Settings -> Secrets del app, con tu DATABASE_URL real de Neon.
# En local no hace falta — app/db.py ya lee el .env de la raíz del repo.
DATABASE_URL = "postgresql://user:password@ep-xxxx.region.aws.neon.tech/observatorio?sslmode=require"
